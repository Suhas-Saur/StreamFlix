# TMDB API Key (Optional)
# If provided, StreamFlix will fetch live movie data and trailers from TMDB API.
# If omitted or invalid, the app automatically falls back to pre-seeded local database.
VITE_TMDB_API_KEY=your_tmdb_api_key_here
